module Strike (input swing, output reg strike);

	always@(*)
		begin
			if (swing)
				begin 
					strike <= 1'b1;
				end
			else 
				begin 
					strike <=1'b0;
				end
		end
		
endmodule
